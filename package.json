{
  "name": "@gpjs-ui/monorepo",
  "private": true,
  "description": "A lightweight Webview-free desktop application framework powered by GPUI and QuickJS.",
  "keywords": [
    "cross-platform",
    "desktop-app",
    "electron-alternative",
    "gpui",
    "gui",
    "quickjs",
    "rust",
    "tauri-alternative",
    "vite",
    "vue",
    "webview-free"
  ],
  "license": "(MIT OR Apache-2.0)",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/tom96da/gpjs-ui.git"
  },
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "pretest": "oxlint --type-aware --type-check && oxfmt --check .",
    "test": "pnpm -r test",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "format": "oxfmt --check .",
    "format:write": "oxfmt --write .",
    "check": "oxlint && oxfmt --check .",
    "typecheck": "oxlint -A all --type-aware --type-check"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@typescript/native": "npm:typescript@^7.0.2",
    "oxfmt": "^0.66.0",
    "oxlint": "^1.81.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "npm:@typescript/typescript6@^6.0.2",
    "unplugin-dts": "^1.1.0",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.2.1",
      "onFail": "download"
    }
  },
  "engines": {
    "node": ">=26"
  },
  "packageManager": "pnpm@12.2.1"
}
