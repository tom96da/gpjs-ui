{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path'); pnpm oxlint --fix --no-error-on-unmatched-pattern \"$file\" 2>/dev/null; pnpm oxfmt --no-error-on-unmatched-pattern \"$file\" 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
